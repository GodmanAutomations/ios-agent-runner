OpenAI Codex v0.98.0 (research preview)
--------
workdir: /Users/stephengodman/ios-agent-runner
model: gpt-5.3-codex
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: xhigh
reasoning summaries: auto
session id: 019c5519-4dd5-7010-accd-cff852479d3b
--------
user
You are executing a handoff ticket. Follow this contract EXACTLY:

1. Read ./_handoff/codex/TICKET.md — this is your assignment.
2. Update ./_handoff/codex/PLAN.md with: objective, repo map notes, plan checklist, progress log, commands run, files changed, remaining work.
3. Execute ONLY what the ticket scope describes. Do NOT touch files listed under "Forbidden Areas".
4. Run every verification command listed in TICKET.md.
5. On SUCCESS:
   - Write ./_handoff/codex/RESULT.md with: summary, files changed, commands run, verification results, any follow-ups.
   - Then create the file ./_handoff/codex/DONE.flag (contents: "done").
   - Do NOT create DONE.flag until ALL verifications pass.
6. On FAILURE:
   - Write ./_handoff/codex/RESULT.md with: failure details, what was attempted, next steps.
   - Then create the file ./_handoff/codex/FAIL.flag (contents: "fail").
7. Always update PLAN.md as you go so progress survives compaction.

BEGIN NOW. Read TICKET.md first.
warning: Under-development features enabled: child_agents_md, exec_policy, remote_compaction. Under-development features are incomplete and may behave unpredictably. To suppress this warning, set `suppress_unstable_features_warning = true` in /Users/stephengodman/.codex/config.toml.
mcp: filesystem starting
mcp: github starting
mcp: memory starting
mcp: context7 starting
mcp: playwright starting
mcp: sequential-thinking starting
mcp: brave-search starting
mcp: codex_apps starting
2026-02-13T03:44:08.445902Z ERROR codex_core::rollout::list: state db missing rollout path for thread 019c403f-1b89-79f3-ac70-bbe35041dbf2
2026-02-13T03:44:08.475102Z ERROR codex_core::rollout::list: state db missing rollout path for thread 019c4308-41ca-7a03-ba30-858b021ed17f
2026-02-13T03:44:08.502426Z ERROR codex_core::rollout::list: state db missing rollout path for thread 019c41c4-40d4-7f23-a3a4-79b611b7114a
2026-02-13T03:44:08.533333Z ERROR codex_core::rollout::list: state db missing rollout path for thread 019c40e4-c271-7692-b921-15ce6f2e122d
2026-02-13T03:44:08.574557Z ERROR codex_core::rollout::list: state db missing rollout path for thread 019c3fea-d430-7e03-81cc-78404de3325d
2026-02-13T03:44:08.637178Z ERROR codex_core::rollout::list: state db missing rollout path for thread 019c4db5-62bd-7861-881c-63c2a4b875df
2026-02-13T03:44:08.666321Z ERROR codex_core::rollout::list: state db missing rollout path for thread 019c4ff9-008d-70f0-aa3a-39d9e85f5cd0
2026-02-13T03:44:08.692148Z ERROR codex_core::rollout::list: state db missing rollout path for thread 019c41ec-3821-7953-b115-affac028cd0c
mcp: codex_apps ready
mcp: brave-search ready
mcp: sequential-thinking ready
mcp: github ready
mcp: filesystem ready
mcp: memory ready
mcp: context7 ready
mcp: playwright ready
mcp startup: ready: codex_apps, brave-search, sequential-thinking, github, filesystem, memory, context7, playwright

thinking
**Starting ticket file inspection**
codex
Reading `./_handoff/codex/TICKET.md` now to determine exact scope, forbidden areas, and required verification commands before making any edits.

thinking
**Reading ticket content**
exec
/bin/zsh -lc 'cat ./_handoff/codex/TICKET.md' in /Users/stephengodman/ios-agent-runner succeeded in 52ms:
# TICKET

## Title
Build comprehensive test suite for scripts/intel.py

## Scope
Write unit tests for ALL public functions in `scripts/intel.py`:
- `extract_all_text(elements)` — text extraction from accessibility elements
- `classify_screen(elements, bundle_id)` — screen category classification
- `extract_structured(texts)` — regex extraction of IPs, MACs, models, firmware, URLs, etc.
- `build_finding(elements, bundle_id, screenshot_path, tree_path, step, goal)` — full pipeline
- `save_finding(finding)` and `load_all_findings()` — JSONL persistence
- `search_findings(query, category, since)` — search/filter

Create: `tests/__init__.py` and `tests/test_intel.py`

Test cases needed:
1. extract_all_text: empty input, all field types (label/name/value/title), dedup, whitespace skip
2. classify_screen: each ScreenCategory has at least one test, bundle_id shortcut for Photos, fallback to APP_UI
3. extract_structured: IPv4, MAC (colon+dash), model numbers (RX-V6A, OLED65C9PUA, LCA003), firmware versions, URLs, ports, resolutions, SSIDs, temperatures, empty input, deduplication

thinking
**Planning structured implementation**
codex
Scope is clear: add only `tests/__init__.py` and `tests/test_intel.py` for all `scripts/intel.py` public functions, then run the two required verification commands. Next I’m reading `PLAN.md` and `scripts/intel.py`, then I’ll write tests and keep `PLAN.md` updated with progress, commands, and file changes.

thinking
**Planning concurrent file reads**
exec
/bin/zsh -lc 'cat ./_handoff/codex/PLAN.md' in /Users/stephengodman/ios-agent-runnerexec
/bin/zsh -lc 'pwd && ls -la' in /Users/stephengodman/ios-agent-runnerexec
/bin/zsh -lc "sed -n '1,260p' scripts/intel.py" in /Users/stephengodman/ios-agent-runner succeeded in 51ms:
# PLAN

## Objective

## Repo Map Notes

## Plan (checklist)
- [ ] Read TICKET.md
- [ ] Discovery
- [ ] Plan
- [ ] Execute
- [ ] Verify

## Progress Log

## Commands Run

## Files Changed

## Remaining Work / Next Actions
 succeeded in 51ms:
/Users/stephengodman/ios-agent-runner
total 168
drwxr-xr-x@  25 stephengodman  staff    800 Feb 12 21:43 .
drwxr-x---+ 160 stephengodman  staff   5120 Feb 12 21:44 ..
drwxr-xr-x@   3 stephengodman  staff     96 Feb 11 16:05 .claude
drwxr-xr-x@   3 stephengodman  staff     96 Feb 12 21:42 .codex
-rw-r--r--@   1 stephengodman  staff    127 Feb 11 16:14 .env
drwxr-xr-x@  12 stephengodman  staff    384 Feb 12 16:31 .git
drwxr-xr-x@   3 stephengodman  staff     96 Feb 12 09:43 .github
-rw-r--r--@   1 stephengodman  staff     53 Feb 11 16:14 .gitignore
drwxr-xr-x@   9 stephengodman  staff    288 Feb 11 14:36 .venv
-rw-r--r--@   1 stephengodman  staff   1490 Feb 12 21:42 AGENTS.md
-rw-r--r--@   1 stephengodman  staff   4960 Feb 11 22:14 CLAUDE.md
-rw-r--r--@   1 stephengodman  staff   1017 Feb 11 15:00 README.md
drwxr-xr-x@   4 stephengodman  staff    128 Feb 12 16:31 __pycache__
drwxr-xr-x@ 571 stephengodman  staff  18272 Feb 12 21:31 _artifacts
drwxr-xr-x@   3 stephengodman  staff     96 Feb 12 21:43 _handoff
-rw-r--r--@   1 stephengodman  staff    608 Feb 11 23:54 demo.tape
-rw-r--r--@   1 stephengodman  staff   1330 Feb 12 09:31 demo_ironman.tape
-rwxr-xr-x@   1 stephengodman  staff   2206 Feb 12 15:42 film_demo.sh
 succeeded in 50ms:
"""Intel Pipeline — classify, extract, persist, and search iOS screen captures.

Every screen the agent sees gets text-extracted, classified, structured-data-mined,
and saved to a JSONL store + a markdown memory file. No caps, no trimming.
"""

import json
import os
import re
import sys
import uuid
from dataclasses import asdict, dataclass, field
from datetime import datetime
from enum import Enum
from pathlib import Path


# ---------------------------------------------------------------------------
# Data models
# ---------------------------------------------------------------------------
exec
/bin/zsh -lc "sed -n '260,520p' scripts/intel.py" in /Users/stephengodman/ios-agent-runner succeeded in 52ms:
def search_findings(
    query: str | None = None,
    category: str | None = None,
    since: str | None = None,
) -> list[dict]:
    """Search by keyword, category, or date. Returns all matches."""
    all_findings = load_all_findings()
    results = []

    for f in all_findings:
        # Category filter
        if category and f.get("category") != category:
            continue

        # Date filter
        if since:
            ts = f.get("timestamp", "")
            if ts < since:
                continue


thinking
**Planning test coverage and documentation updates**
exec
/bin/zsh -lc 'ls -la tests' in /Users/stephengodman/ios-agent-runner exited 1 in 52ms:
ls: tests: No such file or directory

thinking
**Drafting initial plan document**
codex
I’ve mapped `scripts/intel.py` and confirmed there is no existing `tests/` directory. I’m updating `PLAN.md` now with objective, repo notes, checklist, progress, commands already run, and remaining actions, then I’ll implement the test files.

thinking
**Checking file changes**
exec
/bin/zsh -lc 'nl -ba ./_handoff/codex/PLAN.md' in /Users/stephengodman/ios-agent-runner succeeded in 52ms:
     1	# PLAN
     2	
     3	## Objective
     4	
     5	## Repo Map Notes
     6	
     7	## Plan (checklist)
     8	- [ ] Read TICKET.md
     9	- [ ] Discovery
    10	- [ ] Plan
    11	- [ ] Execute
    12	- [ ] Verify
    13	
    14	## Progress Log
    15	
    16	## Commands Run
    17	
    18	## Files Changed
    19	
    20	## Remaining Work / Next Actions

thinking
**Choosing full-file patch approach**
codex
`PLAN.md` still had the template state; I’m applying a full targeted rewrite now so the tracker has current objective, plan, logs, and pending work.
file update
M /Users/stephengodman/ios-agent-runner/_handoff/codex/PLAN.md
@@ -3,18 +3,41 @@
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [ ] Execute scoped test implementation
+- [ ] Run required verification commands
+- [ ] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
 
 ## Remaining Work / Next Actions
+- Create `tests/__init__.py`.
+- Implement `tests/test_intel.py` covering all required cases from the ticket.
+- Run required verification commands:
+  - `source .venv/bin/activate && pip install pytest`
+  - `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- Write `_handoff/codex/RESULT.md` and create success/failure flag.
apply_patch(auto_approved=true) exited 0 in 15ms:
Success. Updated the following files:
M /Users/stephengodman/ios-agent-runner/_handoff/codex/PLAN.md
file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..e1bd0b02f093640a6ad77c856e74b19330573665
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,43 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [ ] Execute scoped test implementation
+- [ ] Run required verification commands
+- [ ] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
 
 ## Remaining Work / Next Actions
+- Create `tests/__init__.py`.
+- Implement `tests/test_intel.py` covering all required cases from the ticket.
+- Run required verification commands:
+  - `source .venv/bin/activate && pip install pytest`
+  - `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- Write `_handoff/codex/RESULT.md` and create success/failure flag.

file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..e1bd0b02f093640a6ad77c856e74b19330573665
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,43 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [ ] Execute scoped test implementation
+- [ ] Run required verification commands
+- [ ] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
 
 ## Remaining Work / Next Actions
+- Create `tests/__init__.py`.
+- Implement `tests/test_intel.py` covering all required cases from the ticket.
+- Run required verification commands:
+  - `source .venv/bin/activate && pip install pytest`
+  - `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- Write `_handoff/codex/RESULT.md` and create success/failure flag.


thinking
**Inspecting scripts for test setup**
exec
/bin/zsh -lc 'ls -la scripts' in /Users/stephengodman/ios-agent-runner succeeded in 52ms:
total 232
drwxr-xr-x@ 14 stephengodman  staff    448 Feb 12 21:41 .
drwxr-xr-x@ 25 stephengodman  staff    800 Feb 12 21:43 ..
-rw-r--r--@  1 stephengodman  staff      0 Feb 11 14:40 __init__.py
drwxr-xr-x@ 13 stephengodman  staff    416 Feb 12 21:41 __pycache__
-rw-r--r--@  1 stephengodman  staff  27631 Feb 12 20:34 agent_loop.py
-rw-r--r--@  1 stephengodman  staff   4780 Feb 12 00:01 device_config.py
-rw-r--r--@  1 stephengodman  staff   9626 Feb 11 23:52 idbwrap.py
-rw-r--r--@  1 stephengodman  staff  12779 Feb 12 20:35 intel.py
-rw-r--r--@  1 stephengodman  staff   5143 Feb 11 14:59 navigator.py
-rw-r--r--@  1 stephengodman  staff   5266 Feb 12 21:30 photo_sweep.py
-rw-r--r--@  1 stephengodman  staff   9035 Feb 11 14:57 screen_mapper.py
-rw-r--r--@  1 stephengodman  staff   2781 Feb 12 20:33 screenshot.py
-rw-r--r--@  1 stephengodman  staff   4423 Feb 11 14:57 simctl.py
-rw-r--r--@  1 stephengodman  staff   9942 Feb 12 21:41 vision_extract.py
file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..e1bd0b02f093640a6ad77c856e74b19330573665
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,43 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [ ] Execute scoped test implementation
+- [ ] Run required verification commands
+- [ ] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
 
 ## Remaining Work / Next Actions
+- Create `tests/__init__.py`.
+- Implement `tests/test_intel.py` covering all required cases from the ticket.
+- Run required verification commands:
+  - `source .venv/bin/activate && pip install pytest`
+  - `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- Write `_handoff/codex/RESULT.md` and create success/failure flag.


thinking
**Designing comprehensive test plan**
codex
I’m implementing the tests now: creating `tests/__init__.py` and a comprehensive `tests/test_intel.py` that covers each required function and edge case, including isolated temp-path persistence via `monkeypatch`.

thinking
**Drafting detailed test cases**
file update
A /Users/stephengodman/ios-agent-runner/tests/__init__.py

apply_patch(auto_approved=true) exited 0 in 27ms:
Success. Updated the following files:
A /Users/stephengodman/ios-agent-runner/tests/__init__.py
file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..e1bd0b02f093640a6ad77c856e74b19330573665
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,43 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [ ] Execute scoped test implementation
+- [ ] Run required verification commands
+- [ ] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
 
 ## Remaining Work / Next Actions
+- Create `tests/__init__.py`.
+- Implement `tests/test_intel.py` covering all required cases from the ticket.
+- Run required verification commands:
+  - `source .venv/bin/activate && pip install pytest`
+  - `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- Write `_handoff/codex/RESULT.md` and create success/failure flag.
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+

file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..e1bd0b02f093640a6ad77c856e74b19330573665
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,43 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [ ] Execute scoped test implementation
+- [ ] Run required verification commands
+- [ ] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
 
 ## Remaining Work / Next Actions
+- Create `tests/__init__.py`.
+- Implement `tests/test_intel.py` covering all required cases from the ticket.
+- Run required verification commands:
+  - `source .venv/bin/activate && pip install pytest`
+  - `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- Write `_handoff/codex/RESULT.md` and create success/failure flag.
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+


thinking
**Adding test file**
file update
A /Users/stephengodman/ios-agent-runner/tests/test_intel.py
import json
import re
from datetime import datetime

import pytest

from scripts import intel


@pytest.fixture()
def isolated_paths(tmp_path, monkeypatch):
    store = tmp_path / "ios_intel.jsonl"
    memory = tmp_path / "memory" / "ios-discoveries.md"
    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
    return store, memory


def test_extract_all_text_empty_input():
    assert intel.extract_all_text([]) == []


def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
    elements = [
        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
    ]

    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]


@pytest.mark.parametrize(
    ("elements", "bundle_id", "expected"),
    [
        (
            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
            "",
            intel.ScreenCategory.NETWORK_CONFIG,
        ),
        (
            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
            "",
            intel.ScreenCategory.DEVICE_SETTINGS,
        ),
        (
            [{"label": "Password"}, {"label": "API key"}],
            "",
            intel.ScreenCategory.CREDENTIALS,
        ),
        (
            [{"label": "HDMI output"}, {"label": "120Hz"}],
            "",
            intel.ScreenCategory.AV_CONFIG,
        ),
        (
            [{"label": "Photos"}, {"label": "Albums"}],
            "",
            intel.ScreenCategory.PHOTO_GALLERY,
        ),
    ],
)
def test_classify_screen_pattern_categories(elements, bundle_id, expected):
    assert intel.classify_screen(elements, bundle_id) == expected


def test_classify_screen_photo_bundle_shortcut_wins():
    elements = [{"label": "Nothing matching patterns"}]
    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY


def test_classify_screen_text_content_heuristic():
    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
    elements = [
        {"type": "StaticText", "label": long_text},
        {"type": "StaticText", "label": long_text + "more words"},
        {"type": "StaticText", "label": long_text + "final words"},
        {"type": "Button", "label": "Continue"},
    ]

    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT


def test_classify_screen_falls_back_to_app_ui():
    elements = [{"label": "Tap here"}, {"name": "Continue"}]
    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI


def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
    elements = [{"label": "mysterytoken"}]
    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN


def test_extract_structured_empty_input():
    assert intel.extract_structured([]) == {}


def test_extract_structured_all_patterns_and_deduplication():
    texts = [
        "IP Address: 192.168.0.10",
        "Backup IP 192.168.0.10",
        "MAC: AA:BB:CC:DD:EE:FF",
        "Secondary MAC AA-BB-CC-DD-EE-FF",
        "Model RX-V6A",
        "Model OLED65C9PUA",
        "Model LCA003",
        "Firmware: 1.2.3",
        "ver 1.2.3",
        "Version 2.0.1",
        "Visit https://example.com/setup",
        "Mirror https://example.com/setup",
        "Port: 8080",
        "Resolution 3840x2160",
        "SSID: HomeNet",
        "Network Guest-WiFi",
        "Current temp 72\u00b0C and ambient 23\u00b0F",
    ]

    extracted = intel.extract_structured(texts)

    assert extracted["ips"] == ["192.168.0.10"]
    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
    assert extracted["urls"] == ["https://example.com/setup"]
    assert extracted["ports"] == ["8080"]
    assert extracted["resolutions"] == ["3840x2160"]
    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]


def test_build_finding_sets_expected_fields_category_tags_and_id():
    elements = [
        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
        {"label": "Visit https://example.com/setup"},
    ]

    finding = intel.build_finding(
        elements=elements,
        bundle_id="com.example.settings",
        screenshot_path="shots/screen-01.png",
        tree_path="trees/screen-01.json",
        step=7,
        goal="Collect network settings",
    )

    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
    assert finding.source_app == "com.example.settings"
    assert finding.screenshot_path == "shots/screen-01.png"
    assert finding.tree_path == "trees/screen-01.json"
    assert finding.step == 7
    assert finding.goal == "Collect network settings"
    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
    assert finding.extracted_data["ips"] == ["10.0.0.5"]
    assert finding.extracted_data["ports"] == ["8080"]
    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
    datetime.fromisoformat(finding.timestamp)

    tags = set(finding.tags)
    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
    assert "app:com.example.settings" in tags
    assert "ips:10.0.0.5" in tags
    assert "ports:8080" in tags
    assert "urls:https://example.com/setup" in tags


def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
    store, memory = isolated_paths

    assert intel.load_all_findings() == []

    finding_one = intel.build_finding(
        elements=[{"label": "IP Address 10.0.0.1"}],
        bundle_id="com.example.router",
        screenshot_path="screen-1.png",
        tree_path="tree-1.json",
        step=1,
        goal="First capture",
    )
    finding_two = intel.build_finding(
        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
        bundle_id="com.example.auth",
        screenshot_path="screen-2.png",
        tree_path="tree-2.json",
        step=2,
        goal="Second capture",
    )

    id_one = intel.save_finding(finding_one)
    first_write = store.read_text().splitlines()
    assert len(first_write) == 1

    id_two = intel.save_finding(finding_two)
    second_write = store.read_text().splitlines()
    assert len(second_write) == 2
    assert second_write[0] == first_write[0]

    loaded = intel.load_all_findings()
    assert len(loaded) == 2
    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
    assert memory.exists()
    assert "Total findings: 2" in memory.read_text()


def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
    store, _ = isolated_paths

    seeded = [
        {
            "timestamp": "2025-01-01T10:00:00",
            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
            "source_app": "com.router.app",
            "screenshot_path": "screen-1.png",
            "tree_path": "tree-1.json",
            "text_content": ["Router Token ABC123"],
            "extracted_data": {"ips": ["10.0.0.1"]},
            "tags": ["network_config", "env:lab"],
            "step": 1,
            "goal": "Network audit",
            "finding_id": "111111111111",
        },
        {
            "timestamp": "2025-01-02T10:00:00",
            "category": intel.ScreenCategory.CREDENTIALS.value,
            "source_app": "com.auth.app",
            "screenshot_path": "screen-2.png",
            "tree_path": "tree-2.json",
            "text_content": ["Password reset page"],
            "extracted_data": {"urls": ["https://auth.example.com"]},
            "tags": ["credentials", "auth"],
            "step": 2,
            "goal": "Credential capture",
            "finding_id": "222222222222",
        },
        {
            "timestamp": "2025-01-03T10:00:00",
            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
            "source_app": "com.router.app",
            "screenshot_path": "screen-3.png",
            "tree_path": "tree-3.json",
            "text_content": ["Status panel"],
            "extracted_data": {"urls": ["https://status.example.com"]},
            "tags": ["network_config"],
            "step": 3,
            "goal": "Health check",
            "finding_id": "333333333333",
        },
    ]
    store.parent.mkdir(parents=True, exist_ok=True)
    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")

    keyword = intel.search_findings(query="TOKEN")
    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
    since = intel.search_findings(since="2025-01-02T00:00:00")
    combined = intel.search_findings(
        query="status.example.com",
        category=intel.ScreenCategory.NETWORK_CONFIG.value,
        since="2025-01-03T00:00:00",
    )
    case_insensitive = intel.search_findings(query="password")

    assert [item["finding_id"] for item in keyword] == ["111111111111"]
    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
    assert [item["finding_id"] for item in combined] == ["333333333333"]
    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]
apply_patch(auto_approved=true) exited 0 in 16ms:
Success. Updated the following files:
A /Users/stephengodman/ios-agent-runner/tests/test_intel.py
file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..e1bd0b02f093640a6ad77c856e74b19330573665
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,43 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [ ] Execute scoped test implementation
+- [ ] Run required verification commands
+- [ ] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
 
 ## Remaining Work / Next Actions
+- Create `tests/__init__.py`.
+- Implement `tests/test_intel.py` covering all required cases from the ticket.
+- Run required verification commands:
+  - `source .venv/bin/activate && pip install pytest`
+  - `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- Write `_handoff/codex/RESULT.md` and create success/failure flag.
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]

file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..e1bd0b02f093640a6ad77c856e74b19330573665
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,43 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [ ] Execute scoped test implementation
+- [ ] Run required verification commands
+- [ ] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
 
 ## Remaining Work / Next Actions
+- Create `tests/__init__.py`.
+- Implement `tests/test_intel.py` covering all required cases from the ticket.
+- Run required verification commands:
+  - `source .venv/bin/activate && pip install pytest`
+  - `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- Write `_handoff/codex/RESULT.md` and create success/failure flag.
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]


thinking
**Planning iterative updates**
exec
/bin/zsh -lc 'cat ./_handoff/codex/PLAN.md' in /Users/stephengodman/ios-agent-runner succeeded in 52ms:
# PLAN

## Objective
- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.

## Repo Map Notes
- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
- `_handoff/codex/PLAN.md`: live execution tracker.

## Plan (checklist)
- [x] Read TICKET.md
- [x] Discovery of project/test target
- [x] Draft execution plan
- [ ] Execute scoped test implementation
- [ ] Run required verification commands
- [ ] Write `RESULT.md` and completion flag

## Progress Log
file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..e1bd0b02f093640a6ad77c856e74b19330573665
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,43 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [ ] Execute scoped test implementation
+- [ ] Run required verification commands
+- [ ] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
 
 ## Remaining Work / Next Actions
+- Create `tests/__init__.py`.
+- Implement `tests/test_intel.py` covering all required cases from the ticket.
+- Run required verification commands:
+  - `source .venv/bin/activate && pip install pytest`
+  - `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- Write `_handoff/codex/RESULT.md` and create success/failure flag.
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]


thinking
**Updating plan with test status**
codex
Test files are now in place with full coverage for the ticket scope. I’m updating `PLAN.md` progress/checklist and command/file logs before running the required verification commands.

thinking
**Applying patch and updating plan**
file update
M /Users/stephengodman/ios-agent-runner/_handoff/codex/PLAN.md
@@ -15,3 +15,3 @@
 - [x] Draft execution plan
-- [ ] Execute scoped test implementation
+- [x] Execute scoped test implementation
 - [ ] Run required verification commands
@@ -23,2 +23,10 @@
 - Confirmed `tests/` does not exist yet and must be created.
+- Created `tests/__init__.py`.
+- Implemented `tests/test_intel.py` with coverage for:
+  - text extraction edge cases
+  - classification categories + bundle shortcut + fallback
+  - structured regex extraction + dedup
+  - finding construction field/tag/id assertions
+  - JSONL save/load append-only behavior using `tmp_path` and `monkeypatch`
+  - search filtering by keyword/category/since/combined/case-insensitive
 
@@ -32,2 +40,7 @@
 - `nl -ba ./_handoff/codex/PLAN.md`
+- `ls -la scripts`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `apply_patch` (create `tests/__init__.py`)
+- `apply_patch` (create `tests/test_intel.py`)
+- `cat ./_handoff/codex/PLAN.md`
 
@@ -35,6 +48,6 @@
 - `_handoff/codex/PLAN.md`
+- `tests/__init__.py`
+- `tests/test_intel.py`
 
 ## Remaining Work / Next Actions
-- Create `tests/__init__.py`.
-- Implement `tests/test_intel.py` covering all required cases from the ticket.
 - Run required verification commands:
apply_patch(auto_approved=true) exited 0 in 33ms:
Success. Updated the following files:
M /Users/stephengodman/ios-agent-runner/_handoff/codex/PLAN.md
file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..c0eae7764afed0b07360c476fe4f1159d69f464d
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,56 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [x] Execute scoped test implementation
+- [ ] Run required verification commands
+- [ ] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
+- Created `tests/__init__.py`.
+- Implemented `tests/test_intel.py` with coverage for:
+  - text extraction edge cases
+  - classification categories + bundle shortcut + fallback
+  - structured regex extraction + dedup
+  - finding construction field/tag/id assertions
+  - JSONL save/load append-only behavior using `tmp_path` and `monkeypatch`
+  - search filtering by keyword/category/since/combined/case-insensitive
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
+- `ls -la scripts`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `apply_patch` (create `tests/__init__.py`)
+- `apply_patch` (create `tests/test_intel.py`)
+- `cat ./_handoff/codex/PLAN.md`
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
+- `tests/__init__.py`
+- `tests/test_intel.py`
 
 ## Remaining Work / Next Actions
+- Run required verification commands:
+  - `source .venv/bin/activate && pip install pytest`
+  - `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- Write `_handoff/codex/RESULT.md` and create success/failure flag.
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]

file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..c0eae7764afed0b07360c476fe4f1159d69f464d
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,56 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [x] Execute scoped test implementation
+- [ ] Run required verification commands
+- [ ] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
+- Created `tests/__init__.py`.
+- Implemented `tests/test_intel.py` with coverage for:
+  - text extraction edge cases
+  - classification categories + bundle shortcut + fallback
+  - structured regex extraction + dedup
+  - finding construction field/tag/id assertions
+  - JSONL save/load append-only behavior using `tmp_path` and `monkeypatch`
+  - search filtering by keyword/category/since/combined/case-insensitive
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
+- `ls -la scripts`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `apply_patch` (create `tests/__init__.py`)
+- `apply_patch` (create `tests/test_intel.py`)
+- `cat ./_handoff/codex/PLAN.md`
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
+- `tests/__init__.py`
+- `tests/test_intel.py`
 
 ## Remaining Work / Next Actions
+- Run required verification commands:
+  - `source .venv/bin/activate && pip install pytest`
+  - `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- Write `_handoff/codex/RESULT.md` and create success/failure flag.
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]


thinking
**Preparing to install pytest**
codex
Executing the required verification sequence now, starting with the exact environment/setup command from the ticket before running pytest.
exec
/bin/zsh -lc 'source .venv/bin/activate && pip install pytest' in /Users/stephengodman/ios-agent-runner succeeded in 876ms:
Collecting pytest
  Using cached pytest-9.0.2-py3-none-any.whl.metadata (7.6 kB)
Collecting iniconfig>=1.0.1 (from pytest)
  Using cached iniconfig-2.3.0-py3-none-any.whl.metadata (2.5 kB)
Collecting packaging>=22 (from pytest)
  Using cached packaging-26.0-py3-none-any.whl.metadata (3.3 kB)
Collecting pluggy<2,>=1.5 (from pytest)
  Using cached pluggy-1.6.0-py3-none-any.whl.metadata (4.8 kB)
Collecting pygments>=2.7.2 (from pytest)
  Using cached pygments-2.19.2-py3-none-any.whl.metadata (2.5 kB)
Using cached pytest-9.0.2-py3-none-any.whl (374 kB)
Using cached pluggy-1.6.0-py3-none-any.whl (20 kB)
Using cached iniconfig-2.3.0-py3-none-any.whl (7.5 kB)
Using cached packaging-26.0-py3-none-any.whl (74 kB)
Using cached pygments-2.19.2-py3-none-any.whl (1.2 MB)
Installing collected packages: pygments, pluggy, packaging, iniconfig, pytest

Successfully installed iniconfig-2.3.0 packaging-26.0 pluggy-1.6.0 pygments-2.19.2 pytest-9.0.2
file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..c0eae7764afed0b07360c476fe4f1159d69f464d
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,56 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [x] Execute scoped test implementation
+- [ ] Run required verification commands
+- [ ] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
+- Created `tests/__init__.py`.
+- Implemented `tests/test_intel.py` with coverage for:
+  - text extraction edge cases
+  - classification categories + bundle shortcut + fallback
+  - structured regex extraction + dedup
+  - finding construction field/tag/id assertions
+  - JSONL save/load append-only behavior using `tmp_path` and `monkeypatch`
+  - search filtering by keyword/category/since/combined/case-insensitive
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
+- `ls -la scripts`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `apply_patch` (create `tests/__init__.py`)
+- `apply_patch` (create `tests/test_intel.py`)
+- `cat ./_handoff/codex/PLAN.md`
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
+- `tests/__init__.py`
+- `tests/test_intel.py`
 
 ## Remaining Work / Next Actions
+- Run required verification commands:
+  - `source .venv/bin/activate && pip install pytest`
+  - `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- Write `_handoff/codex/RESULT.md` and create success/failure flag.
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]


thinking
**Preparing second verification command**
codex
`pytest` is installed in the project venv. Running the second required verification command now: `python -m pytest tests/test_intel.py -v`.
exec
/bin/zsh -lc 'source .venv/bin/activate && python -m pytest tests/test_intel.py -v' in /Users/stephengodman/ios-agent-runner succeeded in 159ms:
============================= test session starts ==============================
platform darwin -- Python 3.12.12, pytest-9.0.2, pluggy-1.6.0 -- /Users/stephengodman/ios-agent-runner/.venv/bin/python
cachedir: .pytest_cache
rootdir: /Users/stephengodman/ios-agent-runner
plugins: anyio-4.12.1
collecting ... collected 16 items

tests/test_intel.py::test_extract_all_text_empty_input PASSED            [  6%]
tests/test_intel.py::test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace PASSED [ 12%]
tests/test_intel.py::test_classify_screen_pattern_categories[elements0--network_config] PASSED [ 18%]
tests/test_intel.py::test_classify_screen_pattern_categories[elements1--device_settings] PASSED [ 25%]
tests/test_intel.py::test_classify_screen_pattern_categories[elements2--credentials] PASSED [ 31%]
tests/test_intel.py::test_classify_screen_pattern_categories[elements3--av_config] PASSED [ 37%]
tests/test_intel.py::test_classify_screen_pattern_categories[elements4--photo_gallery] PASSED [ 43%]
tests/test_intel.py::test_classify_screen_photo_bundle_shortcut_wins PASSED [ 50%]
tests/test_intel.py::test_classify_screen_text_content_heuristic PASSED  [ 56%]
tests/test_intel.py::test_classify_screen_falls_back_to_app_ui PASSED    [ 62%]
tests/test_intel.py::test_classify_screen_unknown_category_when_unknown_pattern_scores PASSED [ 68%]
tests/test_intel.py::test_extract_structured_empty_input PASSED          [ 75%]
tests/test_intel.py::test_extract_structured_all_patterns_and_deduplication PASSED [ 81%]
file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..c0eae7764afed0b07360c476fe4f1159d69f464d
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,56 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [x] Execute scoped test implementation
+- [ ] Run required verification commands
+- [ ] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
+- Created `tests/__init__.py`.
+- Implemented `tests/test_intel.py` with coverage for:
+  - text extraction edge cases
+  - classification categories + bundle shortcut + fallback
+  - structured regex extraction + dedup
+  - finding construction field/tag/id assertions
+  - JSONL save/load append-only behavior using `tmp_path` and `monkeypatch`
+  - search filtering by keyword/category/since/combined/case-insensitive
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
+- `ls -la scripts`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `apply_patch` (create `tests/__init__.py`)
+- `apply_patch` (create `tests/test_intel.py`)
+- `cat ./_handoff/codex/PLAN.md`
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
+- `tests/__init__.py`
+- `tests/test_intel.py`
 
 ## Remaining Work / Next Actions
+- Run required verification commands:
+  - `source .venv/bin/activate && pip install pytest`
+  - `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- Write `_handoff/codex/RESULT.md` and create success/failure flag.
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]


thinking
**Updating plan with verification status**
codex
All required verification commands passed (`16` tests). I’m recording the verification results in `PLAN.md`, then I’ll write `RESULT.md` and create `DONE.flag`.
file update
M /Users/stephengodman/ios-agent-runner/_handoff/codex/PLAN.md
@@ -16,3 +16,3 @@
 - [x] Execute scoped test implementation
-- [ ] Run required verification commands
+- [x] Run required verification commands
 - [ ] Write `RESULT.md` and completion flag
@@ -31,2 +31,5 @@
   - search filtering by keyword/category/since/combined/case-insensitive
+- Ran `source .venv/bin/activate && pip install pytest` successfully.
+- Ran `source .venv/bin/activate && python -m pytest tests/test_intel.py -v` successfully.
+- Verification result: `16 passed in 0.02s`.
 
@@ -45,2 +48,5 @@
 - `cat ./_handoff/codex/PLAN.md`
+- `source .venv/bin/activate && pip install pytest`
+- `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
 
@@ -52,5 +58,2 @@
 ## Remaining Work / Next Actions
-- Run required verification commands:
-  - `source .venv/bin/activate && pip install pytest`
-  - `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
 - Write `_handoff/codex/RESULT.md` and create success/failure flag.
apply_patch(auto_approved=true) exited 0 in 17ms:
Success. Updated the following files:
M /Users/stephengodman/ios-agent-runner/_handoff/codex/PLAN.md
file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..ed24dcef1dc6842eb08cef3c8c28628dac2175f5
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,59 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [x] Execute scoped test implementation
+- [x] Run required verification commands
+- [ ] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
+- Created `tests/__init__.py`.
+- Implemented `tests/test_intel.py` with coverage for:
+  - text extraction edge cases
+  - classification categories + bundle shortcut + fallback
+  - structured regex extraction + dedup
+  - finding construction field/tag/id assertions
+  - JSONL save/load append-only behavior using `tmp_path` and `monkeypatch`
+  - search filtering by keyword/category/since/combined/case-insensitive
+- Ran `source .venv/bin/activate && pip install pytest` successfully.
+- Ran `source .venv/bin/activate && python -m pytest tests/test_intel.py -v` successfully.
+- Verification result: `16 passed in 0.02s`.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
+- `ls -la scripts`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `apply_patch` (create `tests/__init__.py`)
+- `apply_patch` (create `tests/test_intel.py`)
+- `cat ./_handoff/codex/PLAN.md`
+- `source .venv/bin/activate && pip install pytest`
+- `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
+- `tests/__init__.py`
+- `tests/test_intel.py`
 
 ## Remaining Work / Next Actions
+- Write `_handoff/codex/RESULT.md` and create success/failure flag.
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]

file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..ed24dcef1dc6842eb08cef3c8c28628dac2175f5
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,59 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [x] Execute scoped test implementation
+- [x] Run required verification commands
+- [ ] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
+- Created `tests/__init__.py`.
+- Implemented `tests/test_intel.py` with coverage for:
+  - text extraction edge cases
+  - classification categories + bundle shortcut + fallback
+  - structured regex extraction + dedup
+  - finding construction field/tag/id assertions
+  - JSONL save/load append-only behavior using `tmp_path` and `monkeypatch`
+  - search filtering by keyword/category/since/combined/case-insensitive
+- Ran `source .venv/bin/activate && pip install pytest` successfully.
+- Ran `source .venv/bin/activate && python -m pytest tests/test_intel.py -v` successfully.
+- Verification result: `16 passed in 0.02s`.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
+- `ls -la scripts`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `apply_patch` (create `tests/__init__.py`)
+- `apply_patch` (create `tests/test_intel.py`)
+- `cat ./_handoff/codex/PLAN.md`
+- `source .venv/bin/activate && pip install pytest`
+- `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
+- `tests/__init__.py`
+- `tests/test_intel.py`
 
 ## Remaining Work / Next Actions
+- Write `_handoff/codex/RESULT.md` and create success/failure flag.
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]


thinking
**Creating final RESULT.md log**
exec
/bin/zsh -lc 'ls -la ./_handoff/codex' in /Users/stephengodman/ios-agent-runner succeeded in 52ms:
total 432
drwxr-xr-x@ 8 stephengodman  staff     256 Feb 12 21:44 .
drwxr-xr-x@ 3 stephengodman  staff      96 Feb 12 21:43 ..
-rw-r--r--@ 1 stephengodman  staff       6 Feb 12 21:44 .pid
-rw-r--r--@ 1 stephengodman  staff  157860 Feb 12 21:47 LOG.txt
-rw-r--r--@ 1 stephengodman  staff     190 Feb 12 21:44 METADATA.json
-rw-r--r--@ 1 stephengodman  staff    2434 Feb 12 21:47 PLAN.md
-rw-r--r--@ 1 stephengodman  staff       0 Feb 12 21:44 RESULT.md
-rw-r--r--@ 1 stephengodman  staff    1936 Feb 12 21:43 TICKET.md
file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..ed24dcef1dc6842eb08cef3c8c28628dac2175f5
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,59 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [x] Execute scoped test implementation
+- [x] Run required verification commands
+- [ ] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
+- Created `tests/__init__.py`.
+- Implemented `tests/test_intel.py` with coverage for:
+  - text extraction edge cases
+  - classification categories + bundle shortcut + fallback
+  - structured regex extraction + dedup
+  - finding construction field/tag/id assertions
+  - JSONL save/load append-only behavior using `tmp_path` and `monkeypatch`
+  - search filtering by keyword/category/since/combined/case-insensitive
+- Ran `source .venv/bin/activate && pip install pytest` successfully.
+- Ran `source .venv/bin/activate && python -m pytest tests/test_intel.py -v` successfully.
+- Verification result: `16 passed in 0.02s`.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
+- `ls -la scripts`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `apply_patch` (create `tests/__init__.py`)
+- `apply_patch` (create `tests/test_intel.py`)
+- `cat ./_handoff/codex/PLAN.md`
+- `source .venv/bin/activate && pip install pytest`
+- `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
+- `tests/__init__.py`
+- `tests/test_intel.py`
 
 ## Remaining Work / Next Actions
+- Write `_handoff/codex/RESULT.md` and create success/failure flag.
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]


thinking
**Updating empty result file**
codex
I’m writing `RESULT.md` now with summary, changed files, full command log, and explicit verification results from the passing pytest run.
exec
/bin/zsh -lc "cat > ./_handoff/codex/RESULT.md <<'EOF'
# RESULT

## Summary
Implemented a comprehensive "'`pytest` suite for all public functions in `scripts/intel.py` within the ticket scope only. Added tests for extraction, classification (all categories including fallback and photo bundle shortcut), structured regex parsing, finding construction, JSONL persistence round-trip with append-only validation, and search/filter behavior.

## Files Changed
- `_handoff/codex/PLAN.md`
- `tests/__init__.py`
- `tests/test_intel.py`

## Commands Run
- `cat ./_handoff/codex/TICKET.md`
- `pwd && ls -la`
- `cat ./_handoff/codex/PLAN.md`
- `sed -n '"'1,260p' scripts/intel.py"'`
- `sed -n '"'260,520p' scripts/intel.py"'`
- `ls -la tests`
- `nl -ba ./_handoff/codex/PLAN.md`
- `ls -la scripts`
- `apply_patch` (update `_handoff/codex/PLAN.md`)
- `apply_patch` (create `tests/__init__.py`)
- `apply_patch` (create `tests/test_intel.py`)
- `cat ./_handoff/codex/PLAN.md`
- `source .venv/bin/activate && pip install pytest`
- `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
- `apply_patch` (update `_handoff/codex/PLAN.md`)
- `ls -la ./_handoff/codex`

## Verification Results
- `source .venv/bin/activate && pip install pytest` => success
- `source .venv/bin/activate && python -m pytest tests/test_intel.py -v` => success
- Pytest outcome: `16 passed in 0.02s`

## Follow-ups
- None required for ticket completion.
EOF' in /Users/stephengodman/ios-agent-runner exited 1 in 51ms:
zsh:1: file exists: ./_handoff/codex/RESULT.md
file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..ed24dcef1dc6842eb08cef3c8c28628dac2175f5
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,59 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [x] Execute scoped test implementation
+- [x] Run required verification commands
+- [ ] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
+- Created `tests/__init__.py`.
+- Implemented `tests/test_intel.py` with coverage for:
+  - text extraction edge cases
+  - classification categories + bundle shortcut + fallback
+  - structured regex extraction + dedup
+  - finding construction field/tag/id assertions
+  - JSONL save/load append-only behavior using `tmp_path` and `monkeypatch`
+  - search filtering by keyword/category/since/combined/case-insensitive
+- Ran `source .venv/bin/activate && pip install pytest` successfully.
+- Ran `source .venv/bin/activate && python -m pytest tests/test_intel.py -v` successfully.
+- Verification result: `16 passed in 0.02s`.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
+- `ls -la scripts`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `apply_patch` (create `tests/__init__.py`)
+- `apply_patch` (create `tests/test_intel.py`)
+- `cat ./_handoff/codex/PLAN.md`
+- `source .venv/bin/activate && pip install pytest`
+- `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
+- `tests/__init__.py`
+- `tests/test_intel.py`
 
 ## Remaining Work / Next Actions
+- Write `_handoff/codex/RESULT.md` and create success/failure flag.
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]


thinking
**Adjusting shell overwrite behavior**
codex
Shell has `noclobber` enabled, so I’m rewriting `RESULT.md` with explicit overwrite and then proceeding to create `DONE.flag`.
exec
/bin/zsh -lc "cat >| ./_handoff/codex/RESULT.md <<'EOF'
# RESULT

## Summary
Implemented a comprehensive "'`pytest` suite for all public functions in `scripts/intel.py` within the ticket scope only. Added tests for extraction, classification (all categories including fallback and photo bundle shortcut), structured regex parsing, finding construction, JSONL persistence round-trip with append-only validation, and search/filter behavior.

## Files Changed
- `_handoff/codex/PLAN.md`
- `tests/__init__.py`
- `tests/test_intel.py`

## Commands Run
- `cat ./_handoff/codex/TICKET.md`
- `pwd && ls -la`
- `cat ./_handoff/codex/PLAN.md`
- `sed -n '"'1,260p' scripts/intel.py"'`
- `sed -n '"'260,520p' scripts/intel.py"'`
- `ls -la tests`
- `nl -ba ./_handoff/codex/PLAN.md`
- `ls -la scripts`
- `apply_patch` (update `_handoff/codex/PLAN.md`)
- `apply_patch` (create `tests/__init__.py`)
- `apply_patch` (create `tests/test_intel.py`)
- `cat ./_handoff/codex/PLAN.md`
- `source .venv/bin/activate && pip install pytest`
- `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
- `apply_patch` (update `_handoff/codex/PLAN.md`)
- `ls -la ./_handoff/codex`
- `cat >| ./_handoff/codex/RESULT.md <<'"'EOF' ..."'`

## Verification Results
- `source .venv/bin/activate && pip install pytest` => success
- `source .venv/bin/activate && python -m pytest tests/test_intel.py -v` => success
- Pytest outcome: `16 passed in 0.02s`

## Follow-ups
- None required for ticket completion.
EOF' in /Users/stephengodman/ios-agent-runner succeeded in 52ms:

file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..ed24dcef1dc6842eb08cef3c8c28628dac2175f5
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,59 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [x] Execute scoped test implementation
+- [x] Run required verification commands
+- [ ] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
+- Created `tests/__init__.py`.
+- Implemented `tests/test_intel.py` with coverage for:
+  - text extraction edge cases
+  - classification categories + bundle shortcut + fallback
+  - structured regex extraction + dedup
+  - finding construction field/tag/id assertions
+  - JSONL save/load append-only behavior using `tmp_path` and `monkeypatch`
+  - search filtering by keyword/category/since/combined/case-insensitive
+- Ran `source .venv/bin/activate && pip install pytest` successfully.
+- Ran `source .venv/bin/activate && python -m pytest tests/test_intel.py -v` successfully.
+- Verification result: `16 passed in 0.02s`.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
+- `ls -la scripts`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `apply_patch` (create `tests/__init__.py`)
+- `apply_patch` (create `tests/test_intel.py`)
+- `cat ./_handoff/codex/PLAN.md`
+- `source .venv/bin/activate && pip install pytest`
+- `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
+- `tests/__init__.py`
+- `tests/test_intel.py`
 
 ## Remaining Work / Next Actions
+- Write `_handoff/codex/RESULT.md` and create success/failure flag.
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]


thinking
**Creating completion flag file**
codex
All verifications are complete and passing. Creating `./_handoff/codex/DONE.flag` with the required `done` contents now.
exec
/bin/zsh -lc "printf 'done' >| ./_handoff/codex/DONE.flag" in /Users/stephengodman/ios-agent-runner succeeded in 52ms:

file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..ed24dcef1dc6842eb08cef3c8c28628dac2175f5
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,59 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [x] Execute scoped test implementation
+- [x] Run required verification commands
+- [ ] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
+- Created `tests/__init__.py`.
+- Implemented `tests/test_intel.py` with coverage for:
+  - text extraction edge cases
+  - classification categories + bundle shortcut + fallback
+  - structured regex extraction + dedup
+  - finding construction field/tag/id assertions
+  - JSONL save/load append-only behavior using `tmp_path` and `monkeypatch`
+  - search filtering by keyword/category/since/combined/case-insensitive
+- Ran `source .venv/bin/activate && pip install pytest` successfully.
+- Ran `source .venv/bin/activate && python -m pytest tests/test_intel.py -v` successfully.
+- Verification result: `16 passed in 0.02s`.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
+- `ls -la scripts`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `apply_patch` (create `tests/__init__.py`)
+- `apply_patch` (create `tests/test_intel.py`)
+- `cat ./_handoff/codex/PLAN.md`
+- `source .venv/bin/activate && pip install pytest`
+- `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
+- `tests/__init__.py`
+- `tests/test_intel.py`
 
 ## Remaining Work / Next Actions
+- Write `_handoff/codex/RESULT.md` and create success/failure flag.
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]


thinking
**Finalizing result and done flags**
codex
I’m doing the final `PLAN.md` update now to capture `RESULT.md`, `DONE.flag`, and mark the checklist fully complete for handoff durability.
file update
M /Users/stephengodman/ios-agent-runner/_handoff/codex/PLAN.md
@@ -17,3 +17,3 @@
 - [x] Run required verification commands
-- [ ] Write `RESULT.md` and completion flag
+- [x] Write `RESULT.md` and completion flag
 
@@ -34,2 +34,4 @@
 - Verification result: `16 passed in 0.02s`.
+- Wrote `_handoff/codex/RESULT.md` with summary, files changed, commands run, and verification results.
+- Created `_handoff/codex/DONE.flag` with required content `done`.
 
@@ -51,2 +53,7 @@
 - `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `ls -la ./_handoff/codex`
+- `cat > ./_handoff/codex/RESULT.md <<'EOF' ...` (failed due `noclobber`)
+- `cat >| ./_handoff/codex/RESULT.md <<'EOF' ...`
+- `printf 'done' >| ./_handoff/codex/DONE.flag`
+- `apply_patch` (final update `_handoff/codex/PLAN.md`)
 
@@ -54,2 +61,4 @@
 - `_handoff/codex/PLAN.md`
+- `_handoff/codex/RESULT.md`
+- `_handoff/codex/DONE.flag`
 - `tests/__init__.py`
@@ -58,2 +67,2 @@
 ## Remaining Work / Next Actions
-- Write `_handoff/codex/RESULT.md` and create success/failure flag.
+- None.
apply_patch(auto_approved=true) exited 0 in 18ms:
Success. Updated the following files:
M /Users/stephengodman/ios-agent-runner/_handoff/codex/PLAN.md
file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..16b278d2367bb49297eb131adec021a90dc1e4df
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,68 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [x] Execute scoped test implementation
+- [x] Run required verification commands
+- [x] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
+- Created `tests/__init__.py`.
+- Implemented `tests/test_intel.py` with coverage for:
+  - text extraction edge cases
+  - classification categories + bundle shortcut + fallback
+  - structured regex extraction + dedup
+  - finding construction field/tag/id assertions
+  - JSONL save/load append-only behavior using `tmp_path` and `monkeypatch`
+  - search filtering by keyword/category/since/combined/case-insensitive
+- Ran `source .venv/bin/activate && pip install pytest` successfully.
+- Ran `source .venv/bin/activate && python -m pytest tests/test_intel.py -v` successfully.
+- Verification result: `16 passed in 0.02s`.
+- Wrote `_handoff/codex/RESULT.md` with summary, files changed, commands run, and verification results.
+- Created `_handoff/codex/DONE.flag` with required content `done`.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
+- `ls -la scripts`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `apply_patch` (create `tests/__init__.py`)
+- `apply_patch` (create `tests/test_intel.py`)
+- `cat ./_handoff/codex/PLAN.md`
+- `source .venv/bin/activate && pip install pytest`
+- `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `ls -la ./_handoff/codex`
+- `cat > ./_handoff/codex/RESULT.md <<'EOF' ...` (failed due `noclobber`)
+- `cat >| ./_handoff/codex/RESULT.md <<'EOF' ...`
+- `printf 'done' >| ./_handoff/codex/DONE.flag`
+- `apply_patch` (final update `_handoff/codex/PLAN.md`)
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
+- `_handoff/codex/RESULT.md`
+- `_handoff/codex/DONE.flag`
+- `tests/__init__.py`
+- `tests/test_intel.py`
 
 ## Remaining Work / Next Actions
+- None.
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]

file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..16b278d2367bb49297eb131adec021a90dc1e4df
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,68 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [x] Execute scoped test implementation
+- [x] Run required verification commands
+- [x] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
+- Created `tests/__init__.py`.
+- Implemented `tests/test_intel.py` with coverage for:
+  - text extraction edge cases
+  - classification categories + bundle shortcut + fallback
+  - structured regex extraction + dedup
+  - finding construction field/tag/id assertions
+  - JSONL save/load append-only behavior using `tmp_path` and `monkeypatch`
+  - search filtering by keyword/category/since/combined/case-insensitive
+- Ran `source .venv/bin/activate && pip install pytest` successfully.
+- Ran `source .venv/bin/activate && python -m pytest tests/test_intel.py -v` successfully.
+- Verification result: `16 passed in 0.02s`.
+- Wrote `_handoff/codex/RESULT.md` with summary, files changed, commands run, and verification results.
+- Created `_handoff/codex/DONE.flag` with required content `done`.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
+- `ls -la scripts`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `apply_patch` (create `tests/__init__.py`)
+- `apply_patch` (create `tests/test_intel.py`)
+- `cat ./_handoff/codex/PLAN.md`
+- `source .venv/bin/activate && pip install pytest`
+- `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `ls -la ./_handoff/codex`
+- `cat > ./_handoff/codex/RESULT.md <<'EOF' ...` (failed due `noclobber`)
+- `cat >| ./_handoff/codex/RESULT.md <<'EOF' ...`
+- `printf 'done' >| ./_handoff/codex/DONE.flag`
+- `apply_patch` (final update `_handoff/codex/PLAN.md`)
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
+- `_handoff/codex/RESULT.md`
+- `_handoff/codex/DONE.flag`
+- `tests/__init__.py`
+- `tests/test_intel.py`
 
 ## Remaining Work / Next Actions
+- None.
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]


thinking
**Verifying output files**
codex
I’m quickly validating the handoff artifacts (`PLAN.md`, `RESULT.md`, `DONE.flag`) to ensure they were written correctly with the required content.
exec
/bin/zsh -lc 'cat ./_handoff/codex/DONE.flag' in /Users/stephengodman/ios-agent-runnerexec
/bin/zsh -lc "sed -n '1,220p' ./_handoff/codex/RESULT.md" in /Users/stephengodman/ios-agent-runnerexec
/bin/zsh -lc "sed -n '1,260p' ./_handoff/codex/PLAN.md" in /Users/stephengodman/ios-agent-runner succeeded in 51ms:
done
 succeeded in 51ms:
# RESULT

## Summary
Implemented a comprehensive `pytest` suite for all public functions in `scripts/intel.py` within the ticket scope only. Added tests for extraction, classification (all categories including fallback and photo bundle shortcut), structured regex parsing, finding construction, JSONL persistence round-trip with append-only validation, and search/filter behavior.

## Files Changed
- `_handoff/codex/PLAN.md`
- `tests/__init__.py`
- `tests/test_intel.py`

## Commands Run
- `cat ./_handoff/codex/TICKET.md`
- `pwd && ls -la`
- `cat ./_handoff/codex/PLAN.md`
- `sed -n '1,260p' scripts/intel.py`
- `sed -n '260,520p' scripts/intel.py`
- `ls -la tests`
- `nl -ba ./_handoff/codex/PLAN.md`
- `ls -la scripts`
- `apply_patch` (update `_handoff/codex/PLAN.md`)
 succeeded in 51ms:
# PLAN

## Objective
- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.

## Repo Map Notes
- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
- `_handoff/codex/PLAN.md`: live execution tracker.

## Plan (checklist)
- [x] Read TICKET.md
- [x] Discovery of project/test target
- [x] Draft execution plan
- [x] Execute scoped test implementation
- [x] Run required verification commands
- [x] Write `RESULT.md` and completion flag

## Progress Log
file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..16b278d2367bb49297eb131adec021a90dc1e4df
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,68 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [x] Execute scoped test implementation
+- [x] Run required verification commands
+- [x] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
+- Created `tests/__init__.py`.
+- Implemented `tests/test_intel.py` with coverage for:
+  - text extraction edge cases
+  - classification categories + bundle shortcut + fallback
+  - structured regex extraction + dedup
+  - finding construction field/tag/id assertions
+  - JSONL save/load append-only behavior using `tmp_path` and `monkeypatch`
+  - search filtering by keyword/category/since/combined/case-insensitive
+- Ran `source .venv/bin/activate && pip install pytest` successfully.
+- Ran `source .venv/bin/activate && python -m pytest tests/test_intel.py -v` successfully.
+- Verification result: `16 passed in 0.02s`.
+- Wrote `_handoff/codex/RESULT.md` with summary, files changed, commands run, and verification results.
+- Created `_handoff/codex/DONE.flag` with required content `done`.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
+- `ls -la scripts`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `apply_patch` (create `tests/__init__.py`)
+- `apply_patch` (create `tests/test_intel.py`)
+- `cat ./_handoff/codex/PLAN.md`
+- `source .venv/bin/activate && pip install pytest`
+- `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `ls -la ./_handoff/codex`
+- `cat > ./_handoff/codex/RESULT.md <<'EOF' ...` (failed due `noclobber`)
+- `cat >| ./_handoff/codex/RESULT.md <<'EOF' ...`
+- `printf 'done' >| ./_handoff/codex/DONE.flag`
+- `apply_patch` (final update `_handoff/codex/PLAN.md`)
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
+- `_handoff/codex/RESULT.md`
+- `_handoff/codex/DONE.flag`
+- `tests/__init__.py`
+- `tests/test_intel.py`
 
 ## Remaining Work / Next Actions
+- None.
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]


thinking
**Updating RESULT file contents**
codex
I’m making one small correction in `RESULT.md` so its “Files Changed” list fully reflects all artifacts written, including `RESULT.md` and `DONE.flag`.
file update
M /Users/stephengodman/ios-agent-runner/_handoff/codex/RESULT.md
@@ -7,2 +7,4 @@
 - `_handoff/codex/PLAN.md`
+- `_handoff/codex/RESULT.md`
+- `_handoff/codex/DONE.flag`
 - `tests/__init__.py`
apply_patch(auto_approved=true) exited 0 in 20ms:
Success. Updated the following files:
M /Users/stephengodman/ios-agent-runner/_handoff/codex/RESULT.md
file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..16b278d2367bb49297eb131adec021a90dc1e4df
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,68 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [x] Execute scoped test implementation
+- [x] Run required verification commands
+- [x] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
+- Created `tests/__init__.py`.
+- Implemented `tests/test_intel.py` with coverage for:
+  - text extraction edge cases
+  - classification categories + bundle shortcut + fallback
+  - structured regex extraction + dedup
+  - finding construction field/tag/id assertions
+  - JSONL save/load append-only behavior using `tmp_path` and `monkeypatch`
+  - search filtering by keyword/category/since/combined/case-insensitive
+- Ran `source .venv/bin/activate && pip install pytest` successfully.
+- Ran `source .venv/bin/activate && python -m pytest tests/test_intel.py -v` successfully.
+- Verification result: `16 passed in 0.02s`.
+- Wrote `_handoff/codex/RESULT.md` with summary, files changed, commands run, and verification results.
+- Created `_handoff/codex/DONE.flag` with required content `done`.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
+- `ls -la scripts`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `apply_patch` (create `tests/__init__.py`)
+- `apply_patch` (create `tests/test_intel.py`)
+- `cat ./_handoff/codex/PLAN.md`
+- `source .venv/bin/activate && pip install pytest`
+- `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `ls -la ./_handoff/codex`
+- `cat > ./_handoff/codex/RESULT.md <<'EOF' ...` (failed due `noclobber`)
+- `cat >| ./_handoff/codex/RESULT.md <<'EOF' ...`
+- `printf 'done' >| ./_handoff/codex/DONE.flag`
+- `apply_patch` (final update `_handoff/codex/PLAN.md`)
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
+- `_handoff/codex/RESULT.md`
+- `_handoff/codex/DONE.flag`
+- `tests/__init__.py`
+- `tests/test_intel.py`
 
 ## Remaining Work / Next Actions
+- None.
diff --git a/_handoff/codex/RESULT.md b/_handoff/codex/RESULT.md
index 6240c2c346f2d2b0ccfe505b5c7cfedfbe244ea9..365117e33dee9a909a87bf67978bb7ea088f20e2
--- a/_handoff/codex/RESULT.md
+++ b/_handoff/codex/RESULT.md
@@ -5,6 +5,8 @@
 
 ## Files Changed
 - `_handoff/codex/PLAN.md`
+- `_handoff/codex/RESULT.md`
+- `_handoff/codex/DONE.flag`
 - `tests/__init__.py`
 - `tests/test_intel.py`
 
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]

file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..16b278d2367bb49297eb131adec021a90dc1e4df
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,68 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [x] Execute scoped test implementation
+- [x] Run required verification commands
+- [x] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
+- Created `tests/__init__.py`.
+- Implemented `tests/test_intel.py` with coverage for:
+  - text extraction edge cases
+  - classification categories + bundle shortcut + fallback
+  - structured regex extraction + dedup
+  - finding construction field/tag/id assertions
+  - JSONL save/load append-only behavior using `tmp_path` and `monkeypatch`
+  - search filtering by keyword/category/since/combined/case-insensitive
+- Ran `source .venv/bin/activate && pip install pytest` successfully.
+- Ran `source .venv/bin/activate && python -m pytest tests/test_intel.py -v` successfully.
+- Verification result: `16 passed in 0.02s`.
+- Wrote `_handoff/codex/RESULT.md` with summary, files changed, commands run, and verification results.
+- Created `_handoff/codex/DONE.flag` with required content `done`.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
+- `ls -la scripts`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `apply_patch` (create `tests/__init__.py`)
+- `apply_patch` (create `tests/test_intel.py`)
+- `cat ./_handoff/codex/PLAN.md`
+- `source .venv/bin/activate && pip install pytest`
+- `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `ls -la ./_handoff/codex`
+- `cat > ./_handoff/codex/RESULT.md <<'EOF' ...` (failed due `noclobber`)
+- `cat >| ./_handoff/codex/RESULT.md <<'EOF' ...`
+- `printf 'done' >| ./_handoff/codex/DONE.flag`
+- `apply_patch` (final update `_handoff/codex/PLAN.md`)
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
+- `_handoff/codex/RESULT.md`
+- `_handoff/codex/DONE.flag`
+- `tests/__init__.py`
+- `tests/test_intel.py`
 
 ## Remaining Work / Next Actions
+- None.
diff --git a/_handoff/codex/RESULT.md b/_handoff/codex/RESULT.md
index 6240c2c346f2d2b0ccfe505b5c7cfedfbe244ea9..365117e33dee9a909a87bf67978bb7ea088f20e2
--- a/_handoff/codex/RESULT.md
+++ b/_handoff/codex/RESULT.md
@@ -5,6 +5,8 @@
 
 ## Files Changed
 - `_handoff/codex/PLAN.md`
+- `_handoff/codex/RESULT.md`
+- `_handoff/codex/DONE.flag`
 - `tests/__init__.py`
 - `tests/test_intel.py`
 
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]


thinking
**Updating plan with final changes**
codex
`RESULT.md` has been corrected; I’m updating `PLAN.md` one last time to capture that final edit in progress and command history.
file update
M /Users/stephengodman/ios-agent-runner/_handoff/codex/PLAN.md
@@ -36,2 +36,3 @@
 - Created `_handoff/codex/DONE.flag` with required content `done`.
+- Updated `_handoff/codex/RESULT.md` to include all changed artifacts in `Files Changed`.
 
@@ -58,2 +59,7 @@
 - `apply_patch` (final update `_handoff/codex/PLAN.md`)
+- `cat ./_handoff/codex/DONE.flag`
+- `sed -n '1,220p' ./_handoff/codex/RESULT.md`
+- `sed -n '1,260p' ./_handoff/codex/PLAN.md`
+- `apply_patch` (update `_handoff/codex/RESULT.md`)
+- `apply_patch` (final update `_handoff/codex/PLAN.md`)
 
apply_patch(auto_approved=true) exited 0 in 20ms:
Success. Updated the following files:
M /Users/stephengodman/ios-agent-runner/_handoff/codex/PLAN.md
file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..bf6c6bab29e527189472f7c759d30ae0a331bb29
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,74 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [x] Execute scoped test implementation
+- [x] Run required verification commands
+- [x] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
+- Created `tests/__init__.py`.
+- Implemented `tests/test_intel.py` with coverage for:
+  - text extraction edge cases
+  - classification categories + bundle shortcut + fallback
+  - structured regex extraction + dedup
+  - finding construction field/tag/id assertions
+  - JSONL save/load append-only behavior using `tmp_path` and `monkeypatch`
+  - search filtering by keyword/category/since/combined/case-insensitive
+- Ran `source .venv/bin/activate && pip install pytest` successfully.
+- Ran `source .venv/bin/activate && python -m pytest tests/test_intel.py -v` successfully.
+- Verification result: `16 passed in 0.02s`.
+- Wrote `_handoff/codex/RESULT.md` with summary, files changed, commands run, and verification results.
+- Created `_handoff/codex/DONE.flag` with required content `done`.
+- Updated `_handoff/codex/RESULT.md` to include all changed artifacts in `Files Changed`.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
+- `ls -la scripts`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `apply_patch` (create `tests/__init__.py`)
+- `apply_patch` (create `tests/test_intel.py`)
+- `cat ./_handoff/codex/PLAN.md`
+- `source .venv/bin/activate && pip install pytest`
+- `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `ls -la ./_handoff/codex`
+- `cat > ./_handoff/codex/RESULT.md <<'EOF' ...` (failed due `noclobber`)
+- `cat >| ./_handoff/codex/RESULT.md <<'EOF' ...`
+- `printf 'done' >| ./_handoff/codex/DONE.flag`
+- `apply_patch` (final update `_handoff/codex/PLAN.md`)
+- `cat ./_handoff/codex/DONE.flag`
+- `sed -n '1,220p' ./_handoff/codex/RESULT.md`
+- `sed -n '1,260p' ./_handoff/codex/PLAN.md`
+- `apply_patch` (update `_handoff/codex/RESULT.md`)
+- `apply_patch` (final update `_handoff/codex/PLAN.md`)
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
+- `_handoff/codex/RESULT.md`
+- `_handoff/codex/DONE.flag`
+- `tests/__init__.py`
+- `tests/test_intel.py`
 
 ## Remaining Work / Next Actions
+- None.
diff --git a/_handoff/codex/RESULT.md b/_handoff/codex/RESULT.md
index 6240c2c346f2d2b0ccfe505b5c7cfedfbe244ea9..365117e33dee9a909a87bf67978bb7ea088f20e2
--- a/_handoff/codex/RESULT.md
+++ b/_handoff/codex/RESULT.md
@@ -5,6 +5,8 @@
 
 ## Files Changed
 - `_handoff/codex/PLAN.md`
+- `_handoff/codex/RESULT.md`
+- `_handoff/codex/DONE.flag`
 - `tests/__init__.py`
 - `tests/test_intel.py`
 
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]

file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..bf6c6bab29e527189472f7c759d30ae0a331bb29
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,74 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [x] Execute scoped test implementation
+- [x] Run required verification commands
+- [x] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
+- Created `tests/__init__.py`.
+- Implemented `tests/test_intel.py` with coverage for:
+  - text extraction edge cases
+  - classification categories + bundle shortcut + fallback
+  - structured regex extraction + dedup
+  - finding construction field/tag/id assertions
+  - JSONL save/load append-only behavior using `tmp_path` and `monkeypatch`
+  - search filtering by keyword/category/since/combined/case-insensitive
+- Ran `source .venv/bin/activate && pip install pytest` successfully.
+- Ran `source .venv/bin/activate && python -m pytest tests/test_intel.py -v` successfully.
+- Verification result: `16 passed in 0.02s`.
+- Wrote `_handoff/codex/RESULT.md` with summary, files changed, commands run, and verification results.
+- Created `_handoff/codex/DONE.flag` with required content `done`.
+- Updated `_handoff/codex/RESULT.md` to include all changed artifacts in `Files Changed`.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
+- `ls -la scripts`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `apply_patch` (create `tests/__init__.py`)
+- `apply_patch` (create `tests/test_intel.py`)
+- `cat ./_handoff/codex/PLAN.md`
+- `source .venv/bin/activate && pip install pytest`
+- `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `ls -la ./_handoff/codex`
+- `cat > ./_handoff/codex/RESULT.md <<'EOF' ...` (failed due `noclobber`)
+- `cat >| ./_handoff/codex/RESULT.md <<'EOF' ...`
+- `printf 'done' >| ./_handoff/codex/DONE.flag`
+- `apply_patch` (final update `_handoff/codex/PLAN.md`)
+- `cat ./_handoff/codex/DONE.flag`
+- `sed -n '1,220p' ./_handoff/codex/RESULT.md`
+- `sed -n '1,260p' ./_handoff/codex/PLAN.md`
+- `apply_patch` (update `_handoff/codex/RESULT.md`)
+- `apply_patch` (final update `_handoff/codex/PLAN.md`)
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
+- `_handoff/codex/RESULT.md`
+- `_handoff/codex/DONE.flag`
+- `tests/__init__.py`
+- `tests/test_intel.py`
 
 ## Remaining Work / Next Actions
+- None.
diff --git a/_handoff/codex/RESULT.md b/_handoff/codex/RESULT.md
index 6240c2c346f2d2b0ccfe505b5c7cfedfbe244ea9..365117e33dee9a909a87bf67978bb7ea088f20e2
--- a/_handoff/codex/RESULT.md
+++ b/_handoff/codex/RESULT.md
@@ -5,6 +5,8 @@
 
 ## Files Changed
 - `_handoff/codex/PLAN.md`
+- `_handoff/codex/RESULT.md`
+- `_handoff/codex/DONE.flag`
 - `tests/__init__.py`
 - `tests/test_intel.py`
 
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]


thinking
**Confirming final status and changes**
exec
/bin/zsh -lc 'git status --short' in /Users/stephengodman/ios-agent-runnerexec
/bin/zsh -lc "ls -la ./_handoff/codex | rg 'DONE.flag|FAIL.flag|RESULT.md|PLAN.md'" in /Users/stephengodman/ios-agent-runner succeeded in 51ms:
 M main.py
 M mcp_server.py
 M requirements.txt
 M scripts/agent_loop.py
 M scripts/idbwrap.py
 M scripts/screenshot.py
?? .codex/
?? AGENTS.md
?? _handoff/
?? demo.tape
?? demo_ironman.tape
?? film_demo.sh
?? project_audit.json
?? record_demo.sh
?? scripts/device_config.py
?? scripts/intel.py
?? scripts/photo_sweep.py
?? scripts/vision_extract.py
?? seed_demo.sh
?? tests/
 succeeded in 52ms:
-rw-r--r--@ 1 stephengodman  staff       4 Feb 12 21:47 DONE.flag
-rw-r--r--@ 1 stephengodman  staff    3184 Feb 12 21:48 PLAN.md
-rw-r--r--@ 1 stephengodman  staff    1474 Feb 12 21:48 RESULT.md
file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..bf6c6bab29e527189472f7c759d30ae0a331bb29
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,74 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [x] Execute scoped test implementation
+- [x] Run required verification commands
+- [x] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
+- Created `tests/__init__.py`.
+- Implemented `tests/test_intel.py` with coverage for:
+  - text extraction edge cases
+  - classification categories + bundle shortcut + fallback
+  - structured regex extraction + dedup
+  - finding construction field/tag/id assertions
+  - JSONL save/load append-only behavior using `tmp_path` and `monkeypatch`
+  - search filtering by keyword/category/since/combined/case-insensitive
+- Ran `source .venv/bin/activate && pip install pytest` successfully.
+- Ran `source .venv/bin/activate && python -m pytest tests/test_intel.py -v` successfully.
+- Verification result: `16 passed in 0.02s`.
+- Wrote `_handoff/codex/RESULT.md` with summary, files changed, commands run, and verification results.
+- Created `_handoff/codex/DONE.flag` with required content `done`.
+- Updated `_handoff/codex/RESULT.md` to include all changed artifacts in `Files Changed`.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
+- `ls -la scripts`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `apply_patch` (create `tests/__init__.py`)
+- `apply_patch` (create `tests/test_intel.py`)
+- `cat ./_handoff/codex/PLAN.md`
+- `source .venv/bin/activate && pip install pytest`
+- `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `ls -la ./_handoff/codex`
+- `cat > ./_handoff/codex/RESULT.md <<'EOF' ...` (failed due `noclobber`)
+- `cat >| ./_handoff/codex/RESULT.md <<'EOF' ...`
+- `printf 'done' >| ./_handoff/codex/DONE.flag`
+- `apply_patch` (final update `_handoff/codex/PLAN.md`)
+- `cat ./_handoff/codex/DONE.flag`
+- `sed -n '1,220p' ./_handoff/codex/RESULT.md`
+- `sed -n '1,260p' ./_handoff/codex/PLAN.md`
+- `apply_patch` (update `_handoff/codex/RESULT.md`)
+- `apply_patch` (final update `_handoff/codex/PLAN.md`)
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
+- `_handoff/codex/RESULT.md`
+- `_handoff/codex/DONE.flag`
+- `tests/__init__.py`
+- `tests/test_intel.py`
 
 ## Remaining Work / Next Actions
+- None.
diff --git a/_handoff/codex/RESULT.md b/_handoff/codex/RESULT.md
index 6240c2c346f2d2b0ccfe505b5c7cfedfbe244ea9..365117e33dee9a909a87bf67978bb7ea088f20e2
--- a/_handoff/codex/RESULT.md
+++ b/_handoff/codex/RESULT.md
@@ -5,6 +5,8 @@
 
 ## Files Changed
 - `_handoff/codex/PLAN.md`
+- `_handoff/codex/RESULT.md`
+- `_handoff/codex/DONE.flag`
 - `tests/__init__.py`
 - `tests/test_intel.py`
 
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]


thinking
**Confirming completion with verification**
codex
Completed successfully.

- Read `./_handoff/codex/TICKET.md`.
- Kept `./_handoff/codex/PLAN.md` updated with objective, repo map notes, checklist, progress log, commands run, files changed, and remaining work.
- Implemented scoped test files only:
  - `tests/__init__.py`
  - `tests/test_intel.py`
- Ran required verification commands:
  1. `source .venv/bin/activate && pip install pytest` -> success
  2. `source .venv/bin/activate && python -m pytest tests/test_intel.py -v` -> success (`16 passed in 0.02s`)
- Wrote success report to `./_handoff/codex/RESULT.md`.
- Created `./_handoff/codex/DONE.flag` with content `done`.
file update:
diff --git a/_handoff/codex/PLAN.md b/_handoff/codex/PLAN.md
index 8be22641ee7819854260a955ddf9b74e1043870c..bf6c6bab29e527189472f7c759d30ae0a331bb29
--- a/_handoff/codex/PLAN.md
+++ b/_handoff/codex/PLAN.md
@@ -1,20 +1,74 @@
 # PLAN
 
 ## Objective
+- Build a comprehensive `pytest` suite for all public functions in `scripts/intel.py` as defined in `TICKET.md`, without modifying source files.
 
 ## Repo Map Notes
+- `scripts/intel.py`: module under test (`extract_all_text`, `classify_screen`, `extract_structured`, `build_finding`, `save_finding`, `load_all_findings`, `search_findings`).
+- `tests/`: target location for new tests (`__init__.py`, `test_intel.py`).
+- `_handoff/codex/TICKET.md`: assignment scope and required verification commands.
+- `_handoff/codex/PLAN.md`: live execution tracker.
 
 ## Plan (checklist)
-- [ ] Read TICKET.md
-- [ ] Discovery
-- [ ] Plan
-- [ ] Execute
-- [ ] Verify
+- [x] Read TICKET.md
+- [x] Discovery of project/test target
+- [x] Draft execution plan
+- [x] Execute scoped test implementation
+- [x] Run required verification commands
+- [x] Write `RESULT.md` and completion flag
 
 ## Progress Log
+- Read `./_handoff/codex/TICKET.md`.
+- Reviewed `scripts/intel.py` completely to map behaviors and edge cases for each public function.
+- Confirmed `tests/` does not exist yet and must be created.
+- Created `tests/__init__.py`.
+- Implemented `tests/test_intel.py` with coverage for:
+  - text extraction edge cases
+  - classification categories + bundle shortcut + fallback
+  - structured regex extraction + dedup
+  - finding construction field/tag/id assertions
+  - JSONL save/load append-only behavior using `tmp_path` and `monkeypatch`
+  - search filtering by keyword/category/since/combined/case-insensitive
+- Ran `source .venv/bin/activate && pip install pytest` successfully.
+- Ran `source .venv/bin/activate && python -m pytest tests/test_intel.py -v` successfully.
+- Verification result: `16 passed in 0.02s`.
+- Wrote `_handoff/codex/RESULT.md` with summary, files changed, commands run, and verification results.
+- Created `_handoff/codex/DONE.flag` with required content `done`.
+- Updated `_handoff/codex/RESULT.md` to include all changed artifacts in `Files Changed`.
 
 ## Commands Run
+- `cat ./_handoff/codex/TICKET.md`
+- `pwd && ls -la`
+- `cat ./_handoff/codex/PLAN.md`
+- `sed -n '1,260p' scripts/intel.py`
+- `sed -n '260,520p' scripts/intel.py`
+- `ls -la tests`
+- `nl -ba ./_handoff/codex/PLAN.md`
+- `ls -la scripts`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `apply_patch` (create `tests/__init__.py`)
+- `apply_patch` (create `tests/test_intel.py`)
+- `cat ./_handoff/codex/PLAN.md`
+- `source .venv/bin/activate && pip install pytest`
+- `source .venv/bin/activate && python -m pytest tests/test_intel.py -v`
+- `apply_patch` (update `_handoff/codex/PLAN.md`)
+- `ls -la ./_handoff/codex`
+- `cat > ./_handoff/codex/RESULT.md <<'EOF' ...` (failed due `noclobber`)
+- `cat >| ./_handoff/codex/RESULT.md <<'EOF' ...`
+- `printf 'done' >| ./_handoff/codex/DONE.flag`
+- `apply_patch` (final update `_handoff/codex/PLAN.md`)
+- `cat ./_handoff/codex/DONE.flag`
+- `sed -n '1,220p' ./_handoff/codex/RESULT.md`
+- `sed -n '1,260p' ./_handoff/codex/PLAN.md`
+- `apply_patch` (update `_handoff/codex/RESULT.md`)
+- `apply_patch` (final update `_handoff/codex/PLAN.md`)
 
 ## Files Changed
+- `_handoff/codex/PLAN.md`
+- `_handoff/codex/RESULT.md`
+- `_handoff/codex/DONE.flag`
+- `tests/__init__.py`
+- `tests/test_intel.py`
 
 ## Remaining Work / Next Actions
+- None.
diff --git a/_handoff/codex/RESULT.md b/_handoff/codex/RESULT.md
index 6240c2c346f2d2b0ccfe505b5c7cfedfbe244ea9..365117e33dee9a909a87bf67978bb7ea088f20e2
--- a/_handoff/codex/RESULT.md
+++ b/_handoff/codex/RESULT.md
@@ -5,6 +5,8 @@
 
 ## Files Changed
 - `_handoff/codex/PLAN.md`
+- `_handoff/codex/RESULT.md`
+- `_handoff/codex/DONE.flag`
 - `tests/__init__.py`
 - `tests/test_intel.py`
 
diff --git a/tests/__init__.py b/tests/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..8b137891791fe96927ad78e64b0aad7bded08bdc
--- /dev/null
+++ b/tests/__init__.py
@@ -0,0 +1 @@
+
diff --git a/tests/test_intel.py b/tests/test_intel.py
new file mode 100644
index 0000000000000000000000000000000000000000..5b826a71297068e196b327e9eaf318d48d02830d
--- /dev/null
+++ b/tests/test_intel.py
@@ -0,0 +1,268 @@
+import json
+import re
+from datetime import datetime
+
+import pytest
+
+from scripts import intel
+
+
+@pytest.fixture()
+def isolated_paths(tmp_path, monkeypatch):
+    store = tmp_path / "ios_intel.jsonl"
+    memory = tmp_path / "memory" / "ios-discoveries.md"
+    monkeypatch.setattr(intel, "_INTEL_STORE", str(store))
+    monkeypatch.setattr(intel, "_MEMORY_FILE", str(memory))
+    return store, memory
+
+
+def test_extract_all_text_empty_input():
+    assert intel.extract_all_text([]) == []
+
+
+def test_extract_all_text_uses_all_fields_dedups_and_skips_whitespace():
+    elements = [
+        {"label": "  Wi-Fi  ", "name": " Settings ", "value": "", "title": None},
+        {"label": "Wi-Fi", "name": "   ", "value": " Router ", "title": "Router"},
+        {"label": "   ", "name": "Device", "value": 123, "title": "  "},
+    ]
+
+    assert intel.extract_all_text(elements) == ["Wi-Fi", "Settings", "Router", "Device"]
+
+
+@pytest.mark.parametrize(
+    ("elements", "bundle_id", "expected"),
+    [
+        (
+            [{"label": "IP Address 192.168.1.10"}, {"label": "DNS Settings"}],
+            "",
+            intel.ScreenCategory.NETWORK_CONFIG,
+        ),
+        (
+            [{"label": "Model RX-V6A"}, {"label": "Firmware 1.2.3"}],
+            "",
+            intel.ScreenCategory.DEVICE_SETTINGS,
+        ),
+        (
+            [{"label": "Password"}, {"label": "API key"}],
+            "",
+            intel.ScreenCategory.CREDENTIALS,
+        ),
+        (
+            [{"label": "HDMI output"}, {"label": "120Hz"}],
+            "",
+            intel.ScreenCategory.AV_CONFIG,
+        ),
+        (
+            [{"label": "Photos"}, {"label": "Albums"}],
+            "",
+            intel.ScreenCategory.PHOTO_GALLERY,
+        ),
+    ],
+)
+def test_classify_screen_pattern_categories(elements, bundle_id, expected):
+    assert intel.classify_screen(elements, bundle_id) == expected
+
+
+def test_classify_screen_photo_bundle_shortcut_wins():
+    elements = [{"label": "Nothing matching patterns"}]
+    assert intel.classify_screen(elements, "com.apple.mobileslideshow") == intel.ScreenCategory.PHOTO_GALLERY
+
+
+def test_classify_screen_text_content_heuristic():
+    long_text = "lorem ipsum dolor sit amet consectetur adipiscing elit " * 5
+    elements = [
+        {"type": "StaticText", "label": long_text},
+        {"type": "StaticText", "label": long_text + "more words"},
+        {"type": "StaticText", "label": long_text + "final words"},
+        {"type": "Button", "label": "Continue"},
+    ]
+
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.TEXT_CONTENT
+
+
+def test_classify_screen_falls_back_to_app_ui():
+    elements = [{"label": "Tap here"}, {"name": "Continue"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.APP_UI
+
+
+def test_classify_screen_unknown_category_when_unknown_pattern_scores(monkeypatch):
+    monkeypatch.setitem(intel._CATEGORY_PATTERNS, intel.ScreenCategory.UNKNOWN, [r"mysterytoken"])
+    elements = [{"label": "mysterytoken"}]
+    assert intel.classify_screen(elements, "") == intel.ScreenCategory.UNKNOWN
+
+
+def test_extract_structured_empty_input():
+    assert intel.extract_structured([]) == {}
+
+
+def test_extract_structured_all_patterns_and_deduplication():
+    texts = [
+        "IP Address: 192.168.0.10",
+        "Backup IP 192.168.0.10",
+        "MAC: AA:BB:CC:DD:EE:FF",
+        "Secondary MAC AA-BB-CC-DD-EE-FF",
+        "Model RX-V6A",
+        "Model OLED65C9PUA",
+        "Model LCA003",
+        "Firmware: 1.2.3",
+        "ver 1.2.3",
+        "Version 2.0.1",
+        "Visit https://example.com/setup",
+        "Mirror https://example.com/setup",
+        "Port: 8080",
+        "Resolution 3840x2160",
+        "SSID: HomeNet",
+        "Network Guest-WiFi",
+        "Current temp 72\u00b0C and ambient 23\u00b0F",
+    ]
+
+    extracted = intel.extract_structured(texts)
+
+    assert extracted["ips"] == ["192.168.0.10"]
+    assert extracted["macs"] == ["AA:BB:CC:DD:EE:FF", "AA-BB-CC-DD-EE-FF"]
+    assert extracted["model_numbers"] == ["RX-V6A", "OLED65C9PUA", "LCA003"]
+    assert extracted["firmware"] == ["1.2.3", "2.0.1"]
+    assert extracted["urls"] == ["https://example.com/setup"]
+    assert extracted["ports"] == ["8080"]
+    assert extracted["resolutions"] == ["3840x2160"]
+    assert extracted["ssids"] == ["HomeNet", "Guest-WiFi"]
+    assert extracted["temperatures"] == ["72\u00b0C", "23\u00b0F"]
+
+
+def test_build_finding_sets_expected_fields_category_tags_and_id():
+    elements = [
+        {"label": "IP Address 10.0.0.5", "name": "Router", "value": "Port: 8080", "title": "Router"},
+        {"label": "Visit https://example.com/setup"},
+    ]
+
+    finding = intel.build_finding(
+        elements=elements,
+        bundle_id="com.example.settings",
+        screenshot_path="shots/screen-01.png",
+        tree_path="trees/screen-01.json",
+        step=7,
+        goal="Collect network settings",
+    )
+
+    assert finding.category == intel.ScreenCategory.NETWORK_CONFIG.value
+    assert finding.source_app == "com.example.settings"
+    assert finding.screenshot_path == "shots/screen-01.png"
+    assert finding.tree_path == "trees/screen-01.json"
+    assert finding.step == 7
+    assert finding.goal == "Collect network settings"
+    assert finding.text_content == ["IP Address 10.0.0.5", "Router", "Port: 8080", "Visit https://example.com/setup"]
+    assert finding.extracted_data["ips"] == ["10.0.0.5"]
+    assert finding.extracted_data["ports"] == ["8080"]
+    assert finding.extracted_data["urls"] == ["https://example.com/setup"]
+    assert re.fullmatch(r"[0-9a-f]{12}", finding.finding_id)
+    datetime.fromisoformat(finding.timestamp)
+
+    tags = set(finding.tags)
+    assert intel.ScreenCategory.NETWORK_CONFIG.value in tags
+    assert "app:com.example.settings" in tags
+    assert "ips:10.0.0.5" in tags
+    assert "ports:8080" in tags
+    assert "urls:https://example.com/setup" in tags
+
+
+def test_save_and_load_findings_round_trip_with_append_only_behavior(isolated_paths):
+    store, memory = isolated_paths
+
+    assert intel.load_all_findings() == []
+
+    finding_one = intel.build_finding(
+        elements=[{"label": "IP Address 10.0.0.1"}],
+        bundle_id="com.example.router",
+        screenshot_path="screen-1.png",
+        tree_path="tree-1.json",
+        step=1,
+        goal="First capture",
+    )
+    finding_two = intel.build_finding(
+        elements=[{"label": "Password token"}, {"label": "Port: 443"}],
+        bundle_id="com.example.auth",
+        screenshot_path="screen-2.png",
+        tree_path="tree-2.json",
+        step=2,
+        goal="Second capture",
+    )
+
+    id_one = intel.save_finding(finding_one)
+    first_write = store.read_text().splitlines()
+    assert len(first_write) == 1
+
+    id_two = intel.save_finding(finding_two)
+    second_write = store.read_text().splitlines()
+    assert len(second_write) == 2
+    assert second_write[0] == first_write[0]
+
+    loaded = intel.load_all_findings()
+    assert len(loaded) == 2
+    assert [entry["finding_id"] for entry in loaded] == [id_one, id_two]
+    assert memory.exists()
+    assert "Total findings: 2" in memory.read_text()
+
+
+def test_search_findings_filters_keyword_category_since_and_case_insensitive(isolated_paths):
+    store, _ = isolated_paths
+
+    seeded = [
+        {
+            "timestamp": "2025-01-01T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-1.png",
+            "tree_path": "tree-1.json",
+            "text_content": ["Router Token ABC123"],
+            "extracted_data": {"ips": ["10.0.0.1"]},
+            "tags": ["network_config", "env:lab"],
+            "step": 1,
+            "goal": "Network audit",
+            "finding_id": "111111111111",
+        },
+        {
+            "timestamp": "2025-01-02T10:00:00",
+            "category": intel.ScreenCategory.CREDENTIALS.value,
+            "source_app": "com.auth.app",
+            "screenshot_path": "screen-2.png",
+            "tree_path": "tree-2.json",
+            "text_content": ["Password reset page"],
+            "extracted_data": {"urls": ["https://auth.example.com"]},
+            "tags": ["credentials", "auth"],
+            "step": 2,
+            "goal": "Credential capture",
+            "finding_id": "222222222222",
+        },
+        {
+            "timestamp": "2025-01-03T10:00:00",
+            "category": intel.ScreenCategory.NETWORK_CONFIG.value,
+            "source_app": "com.router.app",
+            "screenshot_path": "screen-3.png",
+            "tree_path": "tree-3.json",
+            "text_content": ["Status panel"],
+            "extracted_data": {"urls": ["https://status.example.com"]},
+            "tags": ["network_config"],
+            "step": 3,
+            "goal": "Health check",
+            "finding_id": "333333333333",
+        },
+    ]
+    store.parent.mkdir(parents=True, exist_ok=True)
+    store.write_text("\n".join(json.dumps(item) for item in seeded) + "\n")
+
+    keyword = intel.search_findings(query="TOKEN")
+    category = intel.search_findings(category=intel.ScreenCategory.NETWORK_CONFIG.value)
+    since = intel.search_findings(since="2025-01-02T00:00:00")
+    combined = intel.search_findings(
+        query="status.example.com",
+        category=intel.ScreenCategory.NETWORK_CONFIG.value,
+        since="2025-01-03T00:00:00",
+    )
+    case_insensitive = intel.search_findings(query="password")
+
+    assert [item["finding_id"] for item in keyword] == ["111111111111"]
+    assert [item["finding_id"] for item in category] == ["111111111111", "333333333333"]
+    assert [item["finding_id"] for item in since] == ["222222222222", "333333333333"]
+    assert [item["finding_id"] for item in combined] == ["333333333333"]
+    assert [item["finding_id"] for item in case_insensitive] == ["222222222222"]

tokens used
168,764
Completed successfully.

- Read `./_handoff/codex/TICKET.md`.
- Kept `./_handoff/codex/PLAN.md` updated with objective, repo map notes, checklist, progress log, commands run, files changed, and remaining work.
- Implemented scoped test files only:
  - `tests/__init__.py`
  - `tests/test_intel.py`
- Ran required verification commands:
  1. `source .venv/bin/activate && pip install pytest` -> success
  2. `source .venv/bin/activate && python -m pytest tests/test_intel.py -v` -> success (`16 passed in 0.02s`)
- Wrote success report to `./_handoff/codex/RESULT.md`.
- Created `./_handoff/codex/DONE.flag` with content `done`.
[2026-02-16T04:30:57Z] Started runtime hardening implementation
[2026-02-16T04:30:57Z] Updated optional dependency handling in mcp_server/local_ocr/vision_extract
[2026-02-16T04:30:57Z] Added resilience fixes in scripts/idbwrap.py, scripts/agent_loop.py, scripts/intel.py
[2026-02-16T04:30:57Z] Added tests: test_runtime_hardening.py, test_optional_ocr.py, test_mcp_server_optional.py
[2026-02-16T04:30:57Z] Verification: python -m pytest -v --tb=short => 29 passed
[2026-02-16T04:45:48Z] Committed baseline: chore(runtime): hardening baseline v1
[2026-02-16T04:45:48Z] Tagged baseline: hardening-v1
[2026-02-16T04:52:11Z] Added safe-mode policy, run-state persistence, planner/executor split, and simulator smoke harness
[2026-02-16T04:52:11Z] Added CI workflow: .github/workflows/ci.yml
[2026-02-16T04:52:11Z] Verification: pytest => 39 passed; smoke_simulator => ok
[2026-02-16T04:56:23Z] Fix: smoke harness now runs MCP checks via .venv python and parses clean JSON markers; idbwrap finds .venv/bin/idb from project root
[2026-02-16T04:56:23Z] Verification: pytest => 40 passed; smoke_simulator => ok
[2026-02-16T04:57:13Z] Pushed main + tags to origin via SSH (workflow scope issue over HTTPS)
[2026-02-16T04:57:13Z] origin remote updated to git@github.com:GodmanAutomations/ios-agent-runner.git
[2026-02-16T05:09:04Z] Added dry-run validator + HTML run report generator (CLI + MCP), and persisted artifact paths in run history
[2026-02-16T05:09:04Z] Verification: pytest => 45 passed; smoke_simulator => ok
[2026-02-16T05:17:26Z] Added auto-report on run finalize + latest-run helpers (CLI + MCP)
[2026-02-16T05:17:26Z] Verification: pytest => 47 passed; smoke_simulator => ok
[2026-02-16T06:02:56Z] Tokens confirmed via doctor (all integration env vars present)
[2026-02-16T06:02:56Z] Added Notion/Linear/Sentry/Figma integration modules + MCP tools
[2026-02-16T06:02:56Z] Live validation: notion.me/linear.viewer/sentry.me/figma.me all 200
[2026-02-16T06:02:56Z] Verification: pytest => 53 passed
